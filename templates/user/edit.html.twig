{% extends 'base.html.twig' %}

{% block title %}Edit information{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit your personnal information</h1>
        {{ form_start(form) }}
            {{ form_row(form.email) }}
            <button type="submit" class="btn">Change</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
